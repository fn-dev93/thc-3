{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run development",
            "type": "node-terminal",
            "request": "launch",
            "command": "npm run start:dev",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}